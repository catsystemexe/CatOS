import { test, expect } from "vitest";
import { createUiServer } from "../src/uiServer.js";
test("UI page loads repository-first SETUP without project/profile chrome", async()=>{ const server=createUiServer(); await new Promise<void>(r=>server.listen(0,"127.0.0.1",r)); const addr=server.address(); const port=typeof addr==="object"&&addr?addr.port:0; const res=await fetch(`http://127.0.0.1:${port}/`); const html=await res.text(); expect(res.status).toBe(200); expect(html).toContain("SETUP"); expect(html).toContain("Repository"); expect(html).toContain("Base branch"); expect(html).toContain("PR target"); expect(html).toContain("Task"); expect(html).toContain("RUN"); expect(html).not.toContain("Project status"); expect(html).not.toContain("Profile"); expect(html).not.toContain("Sandbox"); expect(html).not.toContain("F1 Help"); await new Promise<void>(r=>server.close(()=>r())); });
