export const REVIEW_TEMPLATE_VERSION = "review.v1";
export const reviewTemplate = `# CatOS Review Session\n\nReview the current step only. This is a fresh, read-only session: do not modify files, commit, create branches/tags, or use continuation/resume. Return only JSON matching the supplied schema. APPROVE requires no requiredChanges. REWORK requires at least one concrete requiredChange. BLOCKED requires blocker to explicitly name an external dependency or human decision.\n\n## Frozen Task Package\n{{taskPackage}}\n\n## Task / Step Snapshot and Acceptance Criteria\n{{step}}\n\n## Commit Boundaries, Cumulative Diff, Delta Diff, Coding Handoff, and Tests\n{{attempt}}\n\n## Explicit Rework Context\n{{reworkContext}}\n`;
