project:
  id: demo
  name: Demo project
  repoPath: ../demo-project
  baseBranch: main
commands:
  typecheck: npm run typecheck
  test: npm run test
  build: npm run build
workflow:
  maxReworkAttempts: 2
  createCommit: false
permissions:
  allowNetwork: false
  allowPush: false
  allowMerge: false
